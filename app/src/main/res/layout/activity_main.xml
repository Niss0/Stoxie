<?xml version="1.0" encoding="utf-8"?>
<!-- 
    Main Activity Layout - Single Activity Architecture
    This layout implements a single activity pattern with navigation components.
    The NavHostFragment handles fragment navigation while the BottomNavigationView
    provides the main navigation interface.
-->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:fitsSystemWindows="true"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- 
        NavHostFragment - Navigation Container
        This fragment container manages the navigation between different screens.
        It uses the Navigation Component to handle fragment transactions and
        maintains the back stack automatically.
    -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host_fragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:defaultNavHost="true"
        app:layout_constraintBottom_toTopOf="@id/bottom_navigation_view"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:navGraph="@navigation/nav_graph" />

    <!-- 
        BottomNavigationView - Main Navigation Interface
        This Material Design component provides the primary navigation interface.
        It's constrained to the bottom of the screen and automatically handles
        navigation state and visual feedback. The menu resource defines the
        available navigation destinations.
    -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:menu="@menu/bottom_nav_menu" />

</androidx.constraintlayout.widget.ConstraintLayout>
